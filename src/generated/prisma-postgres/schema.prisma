// PostgreSQL veritabanı için schema
generator postgres_client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma-postgres"
}

datasource postgres_db {
  provider = "postgresql"
  url      = env("POSTGRES_DATABASE_URL")
}
